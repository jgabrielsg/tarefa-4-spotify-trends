/** @type {import('next').NextConfig} */
const nextConfig = {
    assetPrefix: '/tarefa-4-spotify-trends', // Certifique-se de que está correto
    basePath: '/tarefa-4-spotify-trends',
    output: 'export',
    reactStrictMode: true,
}

module.exports = nextConfig;
